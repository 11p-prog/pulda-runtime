# CR-0015 2026-07-22 처리 기록 복구

## 기준

- Repository: `11p-prog/pulda-runtime`
- Branch: `main`
- Baseline commit: `d7e4c2868e6e99f77bd8db26eed216548ee0d995`
- Notion: `CR-0015 — ChatGPT Daily Activity Capture`
- Runtime evidence: 2026-07-22 `chatgpt` batch 1건 / 해당
  `external_key` 처리 기록 0건

## 포함 파일

- `pulda/service.py`
- `tests/test_runtime.py`
- `scripts/repair_daily_activity_20260722.py`
- `docs/cr/CR-0015.md`
- `docs/HELP.md`

## 반영 순서

1. ZIP을 로컬 `pulda-runtime` 저장소 루트에 덮어씁니다.
2. 로컬에서 `python -m pytest -q tests/test_runtime.py`를 실행합니다.
3. 예상 결과는 `41 passed`입니다.
4. GitHub Desktop에서 diff를 확인하고 아래 메시지로 commit/push합니다.

   `fix: make daily activity receipt atomic and repair 2026-07-22`

5. Replit에서 최신 `main`을 동기화하고 재배포합니다.
6. Replit Shell에서 드라이런만 실행합니다.

   `python scripts/repair_daily_activity_20260722.py`

7. 출력의 `event_id`, `receipt_count_before`, `source_block_id`를 검토한
   뒤에만 `--apply`를 실행합니다.

## 운영 데이터 안전장치

- 드라이런이 기본이며 운영 DB를 변경하지 않습니다.
- 실행 시 `data/backups/daily-activity-2026-07-22-before-repair.json`을
  먼저 생성합니다. 이 경로는 Git에서 제외됩니다.
- 정확히 일치하는 Notion 봉투가 1건이 아니면 중단합니다.
- 적용은 기존 Event ID를 유지하고 누락된 처리 기록 또는 항목만
  멱등하게 보완합니다.
- 복원 명령:

  `python scripts/repair_daily_activity_20260722.py --restore data/backups/daily-activity-2026-07-22-before-repair.json`

## 검증 결과

- `41 passed`
- 처리 기록 INSERT 실패 시 Event·batch·items 전체 rollback 검증
- 기존 receipt-less batch 재처리 시 Event·items 중복 없음 검증

Status: Prepared locally. 운영 적용 및 User Verified 전에는 Implemented/Closed가 아닙니다.
