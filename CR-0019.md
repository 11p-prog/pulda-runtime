# CR-0019 — Personal Task Log Notion Integration

## Purpose

김경훈님이 매일 노션 "일일 업무 실행 로그" 데이터베이스에 직접 기록하는 개인 업무 실행
데이터(업무명·카테고리·예상/실제 소요시간·상태·자동화 분면)를 Pulda Runtime의 Event
모델로 끌어와, 풀다 OS가 사용자의 실제 업무 패턴을 기반으로 판단할 수 있게 한다.

CR-0015(ChatGPT Daily Activity Capture)와는 목적과 데이터 형태가 다르다. CR-0015는
ChatGPT 세션이 그날 수행한 지시·결정·작업결과를 JSON 봉투로 큐 페이지에 쌓는 AI
활동 로그이고, 이 CR은 사람이 노션 데이터베이스 행(row)에 직접 입력하는 시간축 업무
기록이다. 두 파이프라인은 공존하며 서로 대체하지 않는다.

## Scope

- 노션 데이터소스(`collection://48c5685d-f979-4f61-b854-ad87c1188a8b`, "일일 업무
  실행 로그")를 직접 쿼리하는 새 커넥터를 추가한다. CR-0015의 큐-페이지 방식을
  재사용하지 않는다 — 데이터 형태가 다르기 때문이다.
- 각 노션 행을 Pulda Event 1개에 매핑한다. 아이덴티티(중복 방지 키)는 새 노션 필드를
  추가하지 않고 **노션 페이지 ID**를 그대로 쓴다 — `events.notion_page_id` 컬럼은 이미
  존재한다(`pulda/db.py`).
- 이 소스에만 있는 필드(예상/실제 소요시간, 상태, 자동화 분면, 미루기, 지연일수)는
  `events` 테이블을 오염시키지 않고, `daily_activity_batches`/`daily_activity_items`와
  같은 패턴의 새 부속 테이블에 저장한다(가칭 `notion_task_log_metrics`,
  `event_id`를 참조).
- 노션 "카테고리"(성당/a4u/투어타이/풀다/개인생활/가족/기타/피플라이프 등) →
  `PRJ-*` 프로젝트 코드 매핑은 노션 스키마 변경 없이 커넥터 내부의 정적 매핑 테이블로
  처리한다. 현재 코드에 존재하는 프로젝트 코드는 `PRJ-PULDA-OS` 하나뿐이므로, 나머지
  코드(`PRJ-A4U`, `PRJ-TOURTIE`, `PRJ-CHURCH`, `PRJ-PERSONAL`, `PRJ-FAMILY` 등)는
  이 CR에서 신설한다.
- 온디맨드 풀 엔드포인트를 추가한다: `POST /integrations/notion/task-log/pull`
  (기존 `POST /integrations/notion/daily-activities/pull`과 대칭 구조).
- 노션 DB 스키마 자체는 변경하지 않는다 — 아이덴티티는 페이지 ID로 충분하고,
  프로젝트 코드 매핑은 커넥터 쪽 책임이기 때문이다.

## Approved behavior

- 하나의 노션 페이지는 하나의 Pulda Event로 매핑되며, 재수집해도 중복 Event가
  생기지 않는다(`notion_page_id` upsert).
- 노션 행의 속성이 갱신되면(예: 상태가 "진행 중"→"완료", 실제 소요시간 기록) 같은
  Event와 그 메트릭 레코드를 갱신한다 — 새 Event를 만들지 않는다.
- 필수 필드(업무명, 날짜)가 비어 있는 행은 Event로 만들지 않고 건너뛰며, 건너뛴
  건수를 리포트에 남긴다.
- 카테고리 값이 매핑 테이블에 없으면 오류로 중단하지 않고 `PRJ-UNMAPPED`로 기록하고
  경고를 리포트에 남긴다(운영 중 카테고리가 늘어날 수 있으므로).
- 이 커넥터는 읽기 전용이다 — 노션 페이지를 삭제·재생성하지 않는다. (참고: 자동화
  설계문서 워크시트 R5 규칙에서도 페이지를 유지하도록 이미 못박아 두었다.)

## Acceptance criteria

1. 같은 노션 페이지를 다시 풀(pull)해도 중복 Event가 생기지 않는다.
2. Runtime은 처리된 Event ID 목록과, 생성/갱신 여부를 반환한다.
3. 필수 필드 누락 행은 Event 생성 전에 거부되고 건너뛴 건수로 집계된다.
4. 매핑되지 않은 카테고리는 `PRJ-UNMAPPED`로 기록되고 경고 목록에 남는다.
5. 예상/실제 소요시간·상태·자동화 분면이 `notion_task_log_metrics`에 보존되어
   `events` 테이블 스키마는 이 소스 전용 컬럼으로 오염되지 않는다.
6. 자동화 테스트가 생성/갱신/멱등성(idempotency)/필수 필드 거부/매핑되지 않은
   카테고리 처리를 커버한다.
7. 실제 Runtime 실행과 사용자 검증은 CR-0000 정의대로 별도 완료 게이트로 남는다.

## Implementation (제안 — 미구현)

- `pulda/connectors.py`: `pull_notion_task_log()` — `pull_notion_daily_activities()`와
  나란히 위치. 큐 페이지 블록이 아니라 Notion Data Source 쿼리(SQL 또는 페이지네이션
  API)를 사용.
- `pulda/db.py`: `notion_task_log_metrics` 테이블 추가(additive, 기존 CR들의
  additive-table 관례를 따름) — 최소 컬럼: `event_id`, `notion_page_id`,
  `category`, `project_code`, `expected_minutes`, `actual_minutes`, `status`,
  `automation_quadrant`, `postponed`, `delay_days`, `updated_at`.
- `pulda/service.py`: `capture_notion_task_log_row()` — `capture_daily_activity()`와
  같은 자리에, notion_page_id 기준 upsert 로직으로 구현.
- `pulda/app.py`: `POST /integrations/notion/task-log/pull` 라우트 추가.
- 카테고리→PRJ 코드 매핑 테이블은 커넥터 모듈 내 상수로 시작하고, 카테고리가
  늘어나면 CR 없이 매핑 테이블만 갱신 가능하도록 설계.

## Status

Proposed — 사용자 검토 대기. Owner(ChatGPT/Replit/Claude 중 설계·구현 주체)는
사용자가 배정. 이 CR은 GitHub 저장소에 아직 커밋되지 않았다 — CR-0000 프로토콜에
따라 사용자 승인 후 기존 워크플로우(Replit/Codex/Claude Code)로 진행 예정.

## Related

- CR-0015 — ChatGPT Daily Activity Capture (같은 "일일 활동 수집" 계열이지만 다른
  데이터 소스/파이프라인)
- CR-0016 — Autoscale PostgreSQL Runtime Persistence (이 CR의 새 테이블도 동일한
  이중 백엔드 스키마 패턴을 따라야 함)
- 관련 노션: "일일 업무 실행 로그" 데이터베이스 (`collection://48c5685d-f979-4f61-b854-ad87c1188a8b`),
  "AI Native CEO 오프라인" 자동화 설계문서(일일 업무 정리 루틴)
