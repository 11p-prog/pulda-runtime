# 풀다 OS 개선 방향 — 클로드 코드 인계 브리핑

작성일: 2026-09-03 · 작성 배경: AI Native CEO 오프라인 과정 워크시트 작업 중 파생된 풀다 OS 점검
목적: 이 세션(챗봇 대화)에서 정리된 내용을 클로드 코드가 이어받아 단계적으로 구현 방향을 잡을 수 있도록 인계

---

## 0. 한 줄 요약

풀다 OS는 원래 두 가지 목적으로 만들려던 것이었다 — **① 반복 업무 정리 자동화**, **② 여러 프로젝트의 진행 상태 모니터링**. ①은 이번에 설계문서로 완성했고, ②는 실제로는 "Notion Project Registry + ChatGPT 대화형 감사"로 이미 무겁게 돌아가고 있지만 설계 의도(주 1회 정기 점검)와 실제 운영(사건 발생 시에만, 최대 1개월 지연)이 어긋나 있다는 게 이번 점검의 핵심 발견이다. 아래는 그 갭을 메우기 위한 단계적 개선안이다.

---

## 1. 배경 — 풀다 OS와 사용자

- 사용자(김경훈)는 성당 업무, a4u(Apply4U), 투어타이, 풀다(자사) 등 다중 프로젝트를 1인으로 운영하며, 구글 계정도 개인/회사/성당 3개로 나뉘어 있다(부분적으로만 분리되고 섞이는 편).
- 풀다 OS(Pulda Runtime, GitHub `11p-prog/pulda-runtime`)는 현재 "개발 완료 → 유지보수"로 전환되기 전, 스키마·UI가 자주 바뀌는 **과도기(pre-maintenance/volatile)** 상태다. 이 변동성 때문에 매일 기록해야 하는 실행 데이터는 상대적으로 안정적인 **Notion을 기반 DB로** 두고 있다.
- 거버넌스는 CR-0000 "AI Collaboration Protocol"을 따른다: (기존 구조상) ChatGPT=설계·CR 작성, Replit=구현, 사용자=승인·검증. 이번에 클로드(이 세션 + 클로드 코드)가 새로 개입하는 것이므로, **클로드 코드의 역할을 이 구조 안 어디에 놓을지 사용자가 먼저 정리해줄 필요가 있다** (아래 "8. 아직 결정 안 된 것" 참조).

---

## 2. 이번 세션에서 확인된 것

### A. 완성된 설계문서 1 — "일일 업무 정리 루틴" (반복 업무 자동화)

- 대상: 이메일 확인·정리(개인/회사/성당 3계정), 파일정리, 폰정리, 책상·물품정리, 청소, Notion 로그 기록 — 8단계, 실측 60분/일(9월 로그 기준, 예상 35분 대비 71% 초과).
- 분면 판정: 단계별로 갈림 — 메일정리·파일정리·로그기록=①룰베이스, 메일확인·폰정리=②단순자동화, 책상·물품·청소=자동화 대상 아님(실물 작업). 전체적으로는 "일부 단계만 대체(워크플로우)".
- 가드레일 핵심: 삭제 대신 보관 우선, 개인정보 포함 메일은 자동분류 제외, 파일 이동은 미리보기 승인 필수, **계정(개인/회사/성당) 간 자동 전달·이동 금지**.
- 판정: PoC — 메일정리·파일정리부터 2주 시범.
- 산출물 파일: `오프라인_설계문서_워크시트_김경훈.docx` (사용자 보유)

### B. 완성된 설계문서 2 — "프로젝트 진행관리하기" (프로젝트 모니터링 자동화, 이번 신규)

- **As-Is 실태(사용자 인터뷰 기반, 로그 없음)**: 트리거는 불규칙(불안감·클라이언트 연락 등 사건 기반) → 밀리면 최대 1개월까지 점검 공백 → 점검할 때마다 프로젝트별 GitHub/Imweb/실행화면/이전 문서를 처음부터 다시 훑음(완료 지점 재확인 비용이 병목) → 결과가 Notion에 반영되기도 하고, 파악만 하고 끝나기도 함(완료 조건 없음).
- **5Why 본질 문제**: 프로젝트 상태를 믿고 볼 수 있는 단일 스냅샷이 없어서, 점검 착수 자체가 매번 고비용 재조사가 되고, 그래서 점검이 사건 발생 시로 지연되며 기록도 불완전하게 남는 악순환. (일일 정리 루틴과 동일한 근본 원인: 반복업무 시스템화 시간을 따로 낸 적이 없음.)
- **단계별 분면 판정**: GitHub 정보 수집·Notion 갱신=①룰베이스(자동화 가능), 실행화면 확인=④하이브리드, 이전 문서 재확인·완료 여부 판단·다음 행동 결정=③AI에이전트(판단·맥락 필요). 전체 종합은 **④하이브리드**(자동 수집 + AI 에이전트 초안 + 사람 승인).
- **제안 To-Be 프로세스** (6단계, 사람 개입 표기 없음/검토/승인 3단계):
  1. 트리거 발생 — Auto, 크론(주 1회) — 개입 없음
  2. GitHub 정보 자동 수집 — Auto, public 저장소만 대상, 수집 시각 기록 — 개입 없음
  3. 완료 여부·다음 행동 초안 작성 — AI, 수집 정보+이전 Notion 기록 기반 — 검토
  4. 실행화면 확인 — Human, Imweb·배포 화면 — 검토
  5. 최종 승인 — Human — **승인 (Hooks 설치 1순위 지점)**
  6. Notion 갱신 — Auto, 승인된 내용만 반영 — 개입 없음
- **가드레일(R1~R5)**: 완료 여부·다음 행동 판단은 사람 승인 전까지 Notion 미반영 / 유료 계약·마감 걸린 프로젝트(THAI, ONSTUDIO-WEB 등)는 특히 신중히 재확인 / 자동 수집은 public 저장소만, 개인정보·결제정보 자동 수집·인용 금지 / 수집 정보에 수집 시각 필수 기록 / **Notion 갱신까지 완료돼야 점검 세션 "종료"로 간주** (종료 조건을 명시적으로 정의한 것이 이번 설계의 핵심 개선점).
- **KPI 3개**: 점검 공백 기간(목표: 최대 1개월→1주 이내), Notion 갱신 완료율(목표: 100%), 승인 없이 자동 반영된 상태 변경 건수(목표: 0건).
- **판정**: PoC — GitHub 자동 수집만 2주 시범 적용 후, 판단·갱신 자동화 확장 여부 결정.
- **1주 실행계획**: (D+3) public 저장소 3~4개 대상 GitHub 자동 수집 스크립트 초안 → (D+5) 완료여부·다음행동 초안 스킬/프롬프트 설계 + 사람 승인 흐름 연결 → (D+10) 주 1회 크론 연결, 1주간 점검 공백·Notion 갱신율 재측정.
- 산출물 파일: `오프라인_설계문서_워크시트_프로젝트상태점검_김경훈.docx` (사용자 보유, 이번 세션에서 신규 작성)

### C. Notion Project Registry 실태 조사 (설계 vs 실제 갭)

Notion "Pulda OS Knowledge Repository" 안에 이미 상당히 무거운 프로젝트 관리 구조가 존재함을 확인했다:

- **27 Project Registry**: `PRJ-PULDA-OS`, `PRJ-PULDA-WEB`, `PRJ-HANNAM`(한남동성당), `PRJ-THAI`(투어타이), `PRJ-A4U`, `PRJ-PULDA-MOA`(행사관리), `PRJ-ONSTUDIO-WEB`, `PRJ-MARO`, `PRJ-SEASIDE-WEB`, `PRJ-FOIREA-PETFAIR-WEB`, `PRJ-EUNJO-WEB`, `PRJ-ENTICKET` 등 11개 이상 등록.
- **29 Pulda OS Project Control**: "control tower"가 소유하는 것으로 명시된 범위 — Project Registry 상태, 전체 일정·위험·의존성 모니터링, 진행상황 요약, 프로젝트 간 Learning·Template·Skill 후보 회수, 홈페이지 공개 상태 동기화.
- **정기 감사 상태**: 문서 자체에 "Event-driven micro audit: 즉시 적용 / 정기 통합보고: 주기 미확정 / 권장안: 매주 1회"라고 적혀 있음 — 즉 **설계 의도는 있었으나 강제되는 트리거가 없어 실제로 지켜지지 않고 있었다**는 것이 문서 상으로도 이미 자인되어 있다.
- 각 프로젝트 레코드는 매우 상세하다(Git 커밋 해시, 상태 라벨 `Discovery/Connected/Implemented/Runtime verified/User Verified`, 리스크·차단 요인 등). 다만 이 상세함 자체가 "점검을 시작하는 진입장벽"이 되고 있다는 것이 사용자 인터뷰로 확인된 부분.
- 프로젝트별 저장소 공개 정책: 장기적으로 private을 기본값으로 하되, 필요 시 일회성 public 전환 후 감사. 현재 public인 저장소만 자동 수집 대상으로 삼아야 함(가드레일 R3).
- 유료 계약이 걸린 프로젝트(투어타이/THAI, ONSTUDIO-WEB 등)는 고객 개인정보·결제정보·계약 준수 이슈가 얽혀 있어 자동화 시 특히 주의가 필요.

### D. CR-0019 초안 (별도 트랙 — 아직 미커밋)

이번 세션 이전에 별도로 작성해둔 CR 제안 문서. **B/C와는 다른 데이터 소스**(일일 개인 업무 로그 vs 프로젝트 상태)를 다루므로 혼동하지 않아야 한다.

- 목적: Notion "일일 업무 실행 로그" DB(`collection://48c5685d-f979-4f61-b854-ad87c1188a8b`)에 사용자가 직접 기록하는 개인 업무 실행 데이터를 Pulda Event 모델로 가져오는 것.
- 핵심 설계: `pull_notion_task_log()` 신규 커넥터(CR-0015의 큐-페이지 방식과는 다름, 데이터소스 직접 쿼리) / 아이덴티티는 노션 페이지 ID를 그대로 사용(`events.notion_page_id`, 이미 스키마에 존재) / 소요시간·상태·자동화분면 등 이 소스 전용 필드는 신규 부속 테이블 `notion_task_log_metrics`에 저장(events 테이블 오염 방지) / 카테고리→PRJ 코드 매핑은 커넥터 내부 정적 테이블로 처리(노션 스키마 변경 없음) / 신규 엔드포인트 `POST /integrations/notion/task-log/pull`.
- 상태: **(2026-09-11 갱신) Deferred — 아래 §7 참고. 사용자가 재개발 단계임을 이유로 지금은 보류 결정.**
- 전체 문서: `CR-0019.md` (사용자 보유, 이 브리핑과 함께 전달 권장)

---

## 3. 제안하는 단계적 로드맵 (클로드 코드가 검토·조율할 것)

두 트랙(CR-0019: 일일 업무 로그 / 신규: 프로젝트 상태 점검)은 데이터 소스와 목적이 다르므로 **병렬로 진행 가능**하며, 서로 대체하지 않는다.

**트랙 1 — CR-0019 검토 및 구현 착수**
1. CR-0019.md를 CR-0000 형식 기준으로 재검토(필요시 클로드 코드가 보완)
2. 사용자 승인 후 `pulda/connectors.py`에 `pull_notion_task_log()`, `pulda/db.py`에 `notion_task_log_metrics` 테이블 추가
3. 자동화 테스트(생성/갱신/멱등성/필수필드 거부/매핑되지 않은 카테고리) 작성

> **(2026-09-11 갱신)** 트랙 1은 §7-1에 따라 지금 착수하지 않는다 — 아래 §7 참고.

**트랙 2 — 프로젝트 상태 점검 자동화 (신규 설계문서 기반)**
1. **PoC(2주)**: public 저장소(PULDA-WEB, HANNAM, ONSTUDIO-WEB 등 3~4개)를 대상으로 GitHub API 커밋·이슈 자동 수집 스크립트만 먼저 구현 — 판단·갱신 자동화는 아직 하지 않음
2. 수집 정보 기반으로 "완료 여부·다음 행동" 초안을 작성하는 스킬/서브에이전트 설계 — 사람 승인 없이는 어떤 상태도 확정하지 않음
3. 승인 흐름을 Notion Registry 갱신과 연결 (Hooks 설치 지점: 위 To-Be 5단계 "최종 승인")
4. 주 1회 크론 트리거 연결, 점검 공백 기간·Notion 갱신 완료율 KPI 재측정
5. PoC 결과를 보고 Go/Hold 재판정 (Imweb 실행화면 자동 캡처 등 범위 확장 여부 포함)

**공통 원칙**
- 두 트랙 모두 Notion DB의 **기존 스키마는 변경하지 않는다** — 아이덴티티는 이미 있는 필드(페이지 ID)로 충분.
- 두 트랙 모두 **판단·상태확정 단계는 사람 승인 없이 자동화하지 않는다** (이번 세션에서 반복적으로 강조된 원칙).

---

## 4. 반드시 지켜야 할 가드레일 (두 설계문서 + CR-0019 공통)

- 개인정보·계약·결제 정보가 걸린 처리는 자동 삭제·자동 발송·자동 확정을 하지 않는다. 항상 사람 승인 단계를 거친다.
- 계정/저장소 경계를 넘는 자동 전달·복사·이동을 하지 않는다(개인/회사/성당 계정 간, private/public 저장소 간 모두 해당).
- 자동 수집 정보에는 항상 수집 시각을 기록해 신선도를 표시한다.
- 삭제보다 보관(아카이브)을 기본값으로 하고, 되돌리기 어려운 동작은 지연 시간(예: 30일)을 둔다.
- 유료 클라이언트 계약이 걸린 프로젝트(THAI, ONSTUDIO-WEB 등)의 상태·마감 관련 자동 판단은 특히 보수적으로 다룬다.

---

## 5. 참고 자료

- GitHub: `11p-prog/pulda-runtime` (거버넌스 문서: `docs/cr/CR-0000.md` ~ `CR-0018.md`, `docs/cr/STATUS.md`)
- Notion: "일일 업무 실행 로그" DB, "27 Project Registry", "29 Pulda OS Project Control", "Pulda OS Knowledge Repository"
- 설계문서 2건: `오프라인_설계문서_워크시트_김경훈.docx`(일일 업무 정리), `오프라인_설계문서_워크시트_프로젝트상태점검_김경훈.docx`(프로젝트 진행관리)
- CR 제안: `CR-0019.md`

---

## 6. 아직 결정 안 된 것 (클로드 코드와 먼저 정리할 사항)

1. **역할 재배치**: 기존 CR-0000은 ChatGPT=설계, Replit=구현으로 되어 있는데, 이제 클로드 코드가 개입한다면 이 셋의 역할을 어떻게 나눌지 사용자가 먼저 정해야 한다(예: 클로드 코드가 설계+구현을 함께 맡고, ChatGPT/Replit 트랙은 유지·병행할지 대체할지).
2. **CR-0019 소유자**: "Owner(ChatGPT/Replit/Claude 중 설계·구현 주체)는 사용자가 배정" 상태로 남아 있음 — 클로드 코드로 확정할지 확인 필요.
3. **트랙 2(프로젝트 상태 점검)는 아직 정식 CR 문서가 없음** — CR-0019와 같은 형식으로 새 CR(가칭 CR-0020)을 먼저 작성할지, 아니면 클로드 코드가 바로 구현 착수 전 설계 문서화를 진행할지 결정 필요.
4. Imweb은 API가 제한적이라 실행화면 자동 수집(캡처) 난이도가 있음 — 스크린샷 자동화로 갈지, 당분간 수동으로 남길지는 PoC 결과를 보고 판단.

---

## 7. 2026-09-11 업데이트 — "일일 업무 실행 로그" 활용 방향 정리 (CR-0019 보류)

작성 배경: 사용자가 이 노션 DB를 실제로 매일 채워 쓰기 시작하면서 챗봇 세션에서 몇 차례 추가 대화가 진행됐다. 그 사이 클로드 코드 쪽에서는 별도로 Git 히스토리 정합성 문제(`CLAUDE-CODE-RECONCILIATION-REVIEW.md`, 2026-09-11 — Replit 로컬 `main`과 GitHub `origin/main`이 공통 조상 없이 갈라져 있다는 리뷰)가 발견되어 런타임 자체가 재정비 단계에 들어갔다. 이 두 흐름이 겹치면서 사용자가 "지금 이 DB를 클로드 코드/리플릿이 각각 어떻게 다뤄야 할지" 판단을 내리지 못해, 대화 히스토리를 정리해 전달한다. **이 절은 결정을 대신 내리는 것이 아니라 맥락 전달용이다 — 아래에 새로운 작업 지시는 없다.**

### 7-1. CR-0019는 지금 보류한다 (기각 아님)

사용자 결정(2026-09-10, 챗봇 세션): "기존 CR-0019는 무시해. 현재 OS는 재개발 단계여서 의미없다."

→ `pull_notion_task_log()` 커넥터 구현에 지금 착수하지 않는다. CR-0019.md의 설계 아이디어 자체를 폐기한 것은 아니며, **런타임 Git 정합화(위 리컨실리에이션 리뷰)가 먼저 끝난 뒤에야 재논의 대상**이라는 뜻이다. 지금 이 CR을 검토·구현하는 것은 순서가 맞지 않다는 것이 사용자 의도다. (CR-0019.md의 Status 섹션도 이에 맞춰 갱신해둠.)

### 7-2. 이 노션 DB가 왜 만들어졌고 지금 어떻게 쓰이고 있는지

- 원래 계기는 §2-A와 동일 — "AI Native CEO" 오프라인 과정 워크시트("일일 업무 정리 루틴" 자동화 설계)의 산출물로 시작했다. 지금은 설계문서용 예시가 아니라, 사용자가 계속 채워 쓰는 **운영 중인 개인 실행 로그**다.
- 컬럼 구성은 CR-0019.md 작성 시점과 동일(업무명/카테고리/예상·실제 소요시간/상태/자동화 분면/실제 시작·종료 등) — 스키마 변경 없음.
- 2026-09-08~10 사이 정리된 내역(CR-0019.md·§2-A에는 없던 것):
  - "자동화 분면" 선택지에 `자동화 대상 아님`을 추가하고, 설문조사·AI NATIVE CEO 오프라인 강의 만족도 조사·주식매매 3건을 이 값으로 재분류했다. 이 항목들은 자동화 대상이 아니라 전부 사람이 직접 판단해서 처리하는 일이기 때문이다.
  - "일일 업무 정리 루틴" 묶음 항목을 ①룰베이스 → **④하이브리드**로 재분류하고, 메모에 자동화 가능/불가능 하위 작업 구분을 남겼다(메일확인·폰정리·메시지확인·로그기록 등 일부만 자동화 대상).
  - Notion Calendar 앱에서 종일(all-day)로만 표시되던 문제를 해결했다. 원인은 워크스페이스 캘린더 뷰의 CALENDAR BY 설정이 아니라 **Notion Calendar 앱 자체의 "Required date property"(데이터베이스당 전역 1개 설정)**였다 — "실제 시작"으로 맞춘 뒤 정상 표시됨을 확인했다. 이 설정은 같은 DB를 가리키는 뷰가 여러 개여도 전역으로 공유되므로, "예정 시작" 기준 캘린더 뷰는 노션 안에는 별도로 만들어 두었지만 Notion Calendar 사이드바에는 동시에 올릴 수 없었다(둘 중 하나만 유효).
  - 반복 항목을 복제할 때 일부 건에서 시간대가 UTC 기준으로 잡힌 것처럼 보이는 사례가 발견됐다(전수 오류는 아니고 복제·재편집한 일부 건에 한정된 것으로 보임). **일괄 수정은 하지 않기로 했다** — 잘못 잡힌 값을 일괄 보정하려다 이미 맞는 값까지 훼손할 위험이 확인됐기 때문이다. 사용자가 눈에 띄는 대로 직접 고치기로 하고, "이후 OS에 붙일 때 다시 정제"하기로 명시적으로 미뤄뒀다. **→ 나중에 이 DB를 런타임에 연동할 때는 날짜/시간 값을 그대로 신뢰하지 말고, 타임존 정합성을 한 번 더 검증하는 단계를 반드시 넣을 것.**
- 별개로, 같은 상위 페이지 아래에 신규 노션 DB **`연말 신규매출 1천만원 프로젝트`**(collection://cb38446d-e3aa-4898-a285-fef2b8eb3f7a)를 만들었다. "일일 업무 실행 로그"와는 다른 DB이며, 연말까지 신규매출 1천만원을 만들기 위한 영업/수금 트래커다. **풀다 런타임 연동 대상이 아니다** — 매일 자동 체크인(클로드 스케줄 작업)만 붙어 있는 별도 개인 관리 도구로 취급할 것. 헷갈리지 않도록 여기 같이 적어둔다.

### 7-3. 클로드 코드 / 리플릿에게 남기는 것

- 지금 당장 해야 할 작업은 없다. "일일 업무 실행 로그" DB는 계속 노션에서만 운영되고, CR-0019 구현은 보류 상태를 유지한다.
- 위 Git 히스토리 정합화(리컨실리에이션 리뷰)가 먼저 정리되어야, 이 DB를 런타임에 연동할지·언제 할지를 다시 논의할 수 있다 — 순서를 바꾸지 말 것.
- 나중에 이 논의를 다시 열 때 참고할 것:
  1. 스키마는 CR-0019.md 작성 시점과 동일하다(변경 없음).
  2. 자동화 분면 값이 하나 늘었다(`자동화 대상 아님`) — 매핑 테이블을 다시 만든다면 반영할 것.
  3. 날짜/시간 값은 타임존 검증 없이 그대로 신뢰하지 말 것(복제 항목 일부에 UTC 혼입 의심 사례 있음).
  4. `연말 신규매출 1천만원 프로젝트` DB는 별개 트래커이니 연동 범위에서 제외할 것.
